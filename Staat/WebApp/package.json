{
  "name": "@staat/spa",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "gatsby build && yarn run postbuild",
    "postbuild": "syncdir public ../wwwroot -c -do",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "gatsby develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  },
  "dependencies": {
    "gatsby": "^4.2.0",
    "gatsby-plugin-preact": "^6.2.0",
    "preact": "^10.6.1",
    "preact-render-to-string": "^5.1.19",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "prettier": "^2.5.0",
    "sync-directory": "^4.0.7"
  }
}
